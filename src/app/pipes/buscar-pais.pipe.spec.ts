import { BuscarPaisPipe } from './buscar-pais.pipe';

describe('BuscarPaisPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPaisPipe();
    expect(pipe).toBeTruthy();
  });
});
